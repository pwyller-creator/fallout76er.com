# Separate project with its own GitHub repo (pwyller-creator/retirement-tripwires)
/retirement-tripwires/

# Large video assets — deployed via FileZilla, never edited; not worth versioning
*.mp4

# Local Claude Code config, skills, and hooks — not part of the public site repo
.claude/

# Local tooling / diagnostics — not site assets
/har.json
/launch-claude-code.bat

# Search Console coverage exports — local diagnostics, not site assets
/fallout76er.com-Coverage-*/

# Personal data parked in the web folder — not a site asset (also cloud-only in OneDrive, which breaks git mmap)
All2026QAData.xlsm

# OS noise
Thumbs.db
desktop.ini
